git config --global user.name "jumpdiffusion"
git config --global user.email jumpdiffusion@outlook.com

# set vim as text editor used by commands like git commit
# git config --global core.editor vim
# git config --global core.editor nvim

# setup github username
git config --global github.user "jumpdiffusion"